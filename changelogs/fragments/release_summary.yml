---
release_summary: Allow ServiceNow timezone to be passed via parameter (remote_servicenow_timezone) to the plugin. Clarify error message to indicate that it must be set explicitly (that is, not to the system default) in the ServiceNow user record if not passed in via parameter.
